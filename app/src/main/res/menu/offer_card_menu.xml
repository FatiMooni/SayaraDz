<?xml version="1.0" encoding="utf-8"?>
<menu 
      xmlns:android="http://schemas.android.com/apk/res/android"
>
   <item android:title="Offre">
    <group android:menuCategory="system">
        <item
                android:id="@+id/btn_aperçu"
                android:icon="@drawable/offer_tag"
                android:title="Aperçevoir l'annonce"/>
        <item
                android:id="@+id/btn_dcln"
                android:icon="@drawable/canceld"
                android:title="Anuuler cet offre"/>
    </group>
   </item>
    <group android:menuCategory="alternative">
        <item   android:menuCategory="system"
                android:id="@+id/btn_call"
                android:icon="@drawable/phone"
                android:title="Appeler"/>
    </group>

</menu>